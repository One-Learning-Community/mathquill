import type { AutoDict } from '../../services/options';

export const BuiltInOpNames: AutoDict = {}; // the set of operator names like \sin, \cos, etc that

export const TwoWordOpNames = { limsup: 1, liminf: 1, projlim: 1, injlim: 1 };